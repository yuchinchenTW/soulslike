const p=require(process.env.PUPPETEER_PATH||'puppeteer');
(async()=>{const b=await p.launch({executablePath:'C:\\Program Files\\Google\\Chrome\\Application\\chrome.exe',headless:true,args:['--no-sandbox','--enable-unsafe-swiftshader']});try{const page=await b.newPage();await page.goto('http://127.0.0.1:4173/tests/character-view.html',{waitUntil:'networkidle0'});await page.waitForFunction(()=>window.reviewReady);for(const action of ['light','heavy']){console.log(action);for(let t=0;t<.85;t+=.05){const r=await page.evaluate((a,t)=>reviewPose(a,t,'front'),action,t);console.log(t.toFixed(2),r.tip.map(x=>x.toFixed(2)).join(','));}}}finally{await b.close()}})().catch(e=>{console.error(e);process.exitCode=1});
